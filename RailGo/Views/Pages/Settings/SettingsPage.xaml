<Page
    x:Class="RailGo.Views.Pages.Settings.SettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:helpers="using:RailGo.Helpers"
    xmlns:xaml="using:Microsoft.UI.Xaml"
    xmlns:controls="using:CommunityToolkit.WinUI.Controls"
    xmlns:ui="using:CommunityToolkit.WinUI"
    mc:Ignorable="d">
    <Grid>
        <ScrollViewer VerticalScrollMode="Auto" VerticalScrollBarVisibility="Auto">
            <StackPanel x:Name="ContentArea">
                <InfoBar
                    x:Name="BackgroundErrorInfoBar"
                    IsOpen="False"
                    IsClosable="True"
                    Severity="Error"
                    Title="背景设置失败"
                    Margin="{StaticResource XSmallTopMargin}" />

                <!-- 数据源设置 -->
                <StackPanel Margin="{StaticResource SmallTopBottomMargin}">
                    <TextBlock Text="数据源设置" Style="{ThemeResource SubtitleTextBlockStyle}"
                               Margin="{StaticResource XSmallTopMargin}"/>
                    <controls:SettingsCard x:Name="DataSourcesSettingsCard"
                               Click="OnDataSourcesSettingsCardClicked"
                               Description="设置离线数据库，数据源集合，以及第三方数据等"
                               Header="数据源设置"
                               HeaderIcon="{ui:FontIcon Glyph=&#xE8A1;}"
                               IsClickEnabled="True"
                               Margin="{StaticResource XSmallTopMargin}">
                    </controls:SettingsCard>
                </StackPanel>

                <!-- RailGPT API 设置 -->
                <TextBlock Text="RailGPT API 设置" Style="{ThemeResource SubtitleTextBlockStyle}"
                           Margin="{StaticResource XSmallTopMargin}"/>
                <StackPanel Margin="{StaticResource SmallTopBottomMargin}" Spacing="4">
                    <!-- Status -->
                    <InfoBar x:Name="AISettingsInfoBar"
                             IsOpen="False"
                             IsClosable="True"
                             Severity="Informational"
                             Margin="{StaticResource XSmallTopMargin}" />

                    <controls:SettingsCard
                               Description="选择 AI 服务提供商"
                               Header="服务提供商"
                               HeaderIcon="{ui:FontIcon Glyph=&#xE943;}"
                               Margin="{StaticResource XSmallTopMargin}">
                        <ComboBox x:Name="ProviderComboBox"
                                  MinWidth="200"
                                  SelectionChanged="ProviderComboBox_SelectionChanged"
                                  FontSize="14">
                            <ComboBoxItem Content="DeepSeek" Tag="deepseek" />
                        </ComboBox>
                    </controls:SettingsCard>

                    <controls:SettingsCard
                               Description="用于对话生成的 API Key（必填）"
                               Header="主对话 API Key"
                               HeaderIcon="{ui:FontIcon Glyph=&#xE72E;}"
                               Margin="{StaticResource XSmallTopMargin}">
                        <StackPanel Spacing="8" MinWidth="280">
                            <PasswordBox x:Name="PrimaryApiKeyBox"
                                         PlaceholderText="输入 API Key (sk-...)"
                                         FontSize="14" />
                            <TextBlock x:Name="PrimaryKeyStatus"
                                       FontSize="12"
                                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                       Text="未配置" />
                            <StackPanel Orientation="Horizontal" Spacing="8">
                                <Button x:Name="SavePrimaryKeyBtn" Content="保存主对话 Key" Click="OnSavePrimaryKeyClicked" />
                                <Button x:Name="ClearPrimaryKeyBtn" Content="删除" Click="OnClearPrimaryKeyClicked" />
                            </StackPanel>
                        </StackPanel>
                    </controls:SettingsCard>

                    <controls:SettingsCard
                               Description="用于深度思考模式的 API Key（选填，不填时复用主对话 Key）"
                               Header="Thinker API Key"
                               HeaderIcon="{ui:FontIcon Glyph=&#xE7BE;}"
                               Margin="{StaticResource XSmallTopMargin}">
                        <StackPanel Spacing="8" MinWidth="280">
                            <PasswordBox x:Name="ThinkingApiKeyBox"
                                         PlaceholderText="输入 Thinker API Key（可选）"
                                         FontSize="14" />
                            <TextBlock x:Name="ThinkingKeyStatus"
                                       FontSize="12"
                                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                       Text="未配置，将自动复用主对话 Key" />
                            <StackPanel Orientation="Horizontal" Spacing="8">
                                <Button x:Name="SaveThinkingKeyBtn" Content="保存 Thinker Key" Click="OnSaveThinkingKeyClicked" />
                                <Button x:Name="ClearThinkingKeyBtn" Content="删除" Click="OnClearThinkingKeyClicked" />
                            </StackPanel>
                        </StackPanel>
                    </controls:SettingsCard>

                    <controls:SettingsCard
                               Description="加密存储位置"
                               Header="配置文件"
                               HeaderIcon="{ui:FontIcon Glyph=&#xE8A5;}"
                               Margin="{StaticResource XSmallTopMargin}">
                        <TextBlock x:Name="ConfigPathText"
                                   Text="%APPDATA%/RailGPT/settings.enc"
                                   FontSize="12"
                                   TextWrapping="Wrap"
                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                    </controls:SettingsCard>
                </StackPanel>

                <!-- 个性化 -->
                <TextBlock x:Uid="Settings_Personalization" Style="{ThemeResource SubtitleTextBlockStyle}" 
                           Margin="{StaticResource XSmallTopMargin}"/>
                <StackPanel Margin="{StaticResource SmallTopBottomMargin}">
                    <controls:SettingsCard
                               Description="颜色模式选择，选择浅色，深色，或跟随系统"
                               Header="颜色模式选择"
                               HeaderIcon="{ui:FontIcon Glyph=&#xE793;}"
                               Margin="{StaticResource XSmallTopMargin}">
                        <ComboBox
                                x:Name="ThemeComboBox"
                                Margin="{StaticResource XSmallTopMargin}"
                                SelectionChanged="ThemeComboBox_SelectionChanged"
                                FontSize="15">
                            <ComboBoxItem Content="浅色" Tag="Light" />
                            <ComboBoxItem Content="深色" Tag="Dark" />
                            <ComboBoxItem Content="跟随系统" Tag="Default" />
                        </ComboBox>
                    </controls:SettingsCard>
                    <controls:SettingsCard
                               Description="选择本地图片作为软件背景，可随时恢复默认背景"
                               Header="背景图片"
                               HeaderIcon="{ui:FontIcon Glyph=&#xE91B;}"
                               Margin="{StaticResource XSmallTopMargin}">
                        <StackPanel Margin="{StaticResource XSmallTopMargin}" Spacing="8">
                            <StackPanel Orientation="Horizontal" Spacing="8">
                                <Button Content="选择图片" Click="OnChooseBackgroundImageClicked" />
                                <Button x:Name="ClearBackgroundButton" Content="恢复默认" Click="OnClearBackgroundImageClicked" />
                            </StackPanel>
                            <TextBlock
                                x:Name="BackgroundImagePathTextBlock"
                                Text="当前：默认背景"
                                TextWrapping="WrapWholeWords" />
                        </StackPanel>
                    </controls:SettingsCard>
                </StackPanel>

                <!-- 链接与功能 -->
                <TextBlock Text="链接与功能" Style="{ThemeResource SubtitleTextBlockStyle}" 
                           Margin="{StaticResource XSmallTopMargin}"/>
                <StackPanel Margin="{StaticResource SmallTopBottomMargin}">
                    <controls:SettingsCard
                               Description="访问 RailGo 官方网站获取更多信息"
                               Header="打开官网"
                               HeaderIcon="{ui:FontIcon Glyph=&#xE71B;}"
                               Margin="{StaticResource XSmallTopMargin}"
                               IsClickEnabled="True"
                               Click="OnOpenWebsiteClicked">
                    </controls:SettingsCard>
                    <controls:SettingsCard
                               Description="访问 RailGo Center 管理中心"
                               Header="RailGo Center"
                               HeaderIcon="{ui:FontIcon Glyph=&#xE8D6;}"
                               Margin="{StaticResource XSmallTopMargin}"
                               IsClickEnabled="True"
                               Click="OnOpenRailGoCenterClicked">
                    </controls:SettingsCard>
                    <controls:SettingsCard
                               Description="加入官方交流群反馈问题"
                               Header="加入交流群"
                               HeaderIcon="{ui:FontIcon Glyph=&#xE77B;}"
                               Margin="{StaticResource XSmallTopMargin}"
                               IsClickEnabled="True"
                               Click="OnJoinGroupClicked">
                    </controls:SettingsCard>
                </StackPanel>

                <!-- 关于 -->
                <TextBlock x:Uid="Settings_About" Style="{ThemeResource SubtitleTextBlockStyle}" />
                <StackPanel Margin="{StaticResource XSmallTopMargin}">
                    <controls:SettingsCard
                               Header="版本信息"
                               HeaderIcon="{ui:FontIcon Glyph=&#xE7BE;}"
                               Margin="{StaticResource XSmallTopMargin}">
                        <TextBlock Text="{x:Bind ViewModel.VersionDescription, Mode=OneWay}" 
                                   Style="{ThemeResource BodyTextBlockStyle}" />
                    </controls:SettingsCard>
                    <controls:SettingsCard
                               Header="关于 RailGo"
                               HeaderIcon="{ui:FontIcon Glyph=&#xE946;}"
                               Margin="{StaticResource XSmallTopMargin}">
                        <StackPanel>
                            <TextBlock
                                x:Uid="Settings_AboutDescription"
                                Margin="{StaticResource XSmallTopMargin}"
                                Style="{ThemeResource BodyTextBlockStyle}" />
                            <TextBlock
                                Text="AZ Studio 版权所有"
                                Margin="{StaticResource XSmallTopMargin}"
                                Style="{ThemeResource BodyTextBlockStyle}"
                                Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                        </StackPanel>
                    </controls:SettingsCard>
                    <controls:SettingsCard
                               Header="隐私政策"
                               HeaderIcon="{ui:FontIcon Glyph=&#xE72E;}"
                               Margin="{StaticResource XSmallTopMargin}">
                        <HyperlinkButton x:Uid="SettingsPage_PrivacyTermsLink" />
                    </controls:SettingsCard>
                </StackPanel>
            </StackPanel>
        </ScrollViewer>

        <!-- 加入交流群对话框 -->
        <ContentDialog x:Name="JoinGroupDialog"
                       Title="加入交流群"
                       CloseButtonText="关闭"
                       PrimaryButtonText="复制群号"
                       PrimaryButtonClick="OnCopyGroupNumberClicked">
            <StackPanel>
                <TextBlock TextWrapping="WrapWholeWords" Margin="0,0,0,12">
                    复制群号加入群聊【RailGo 官方反馈 | 重拳出击盗版中】652032716
                </TextBlock>
                <TextBlock Text="群号：652032716" 
                           FontWeight="Bold"
                           FontSize="18"
                           HorizontalAlignment="Center"/>
            </StackPanel>
        </ContentDialog>
    </Grid>
</Page>
